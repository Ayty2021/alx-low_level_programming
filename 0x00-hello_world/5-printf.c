#include <stdio.h>
/**
 * Main - entry point
 * Hello, Printf task
 * Return: return 1 after program execution
 */
int main()
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
